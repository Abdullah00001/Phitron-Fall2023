#include <stdio.h>
#include <math.h>
#include <stdlib.h>
int main()
{
    // double a,b,c,d;
    // scanf("%lf %lf %lf %lf",&a,&b,&c,&d);
    // int e,f,g,h;
    // e=ceil(a);
    // f=floor(b);
    // g=round(c);
    // h=round(d);
    // printf("%d\n%d\n%d\n%d\n",e,f,g,h);
    // int r;
    // scanf("%d",&r);
    // int ans=sqrt(r);
    // printf("%d\n",ans);
    // int r;
    // scanf("%d",&r);
    // double ans=sqrt(r);
    // printf("%0.2lf\n",ans);
    int a,b;
    scanf("%d %d",&a,&b);
    int ans=pow(a*1.0,b*1.0);
    printf("%d",ans);
    // double a,b;
    // scanf("%lf %lf",&a,&b);
    // double ans=pow(a,b);
    // printf("%.3lf",ans);
    // int a;
    // scanf("%d",&a);
    // int ans=abs(a);
    // printf("%d",ans);
    return 0;
}